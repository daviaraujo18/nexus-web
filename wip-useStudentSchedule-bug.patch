diff --git a/hooks/useStudentSchedule.ts b/hooks/useStudentSchedule.ts
index 977ee53..3c50596 100644
--- a/hooks/useStudentSchedule.ts
+++ b/hooks/useStudentSchedule.ts
@@ -180,8 +180,21 @@ export function useStudentSchedule() {
     const localToday = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`;
     // JS getDay(): 0=Dom, 1=Seg ... Schedule dayOfWeek: 0=Seg, 1=Ter ...
     // Conversão: (jsDay + 6) % 7
-    const scheduleDayOfWeek = (now.getDay() + 6) % 7;
-
+    const scheduleDayOfWeek = now.getDay()
+    console.log('[TODAY_ACTIVITIES_DIAG]', {
+      today: now.toString(),
+      jsToday: now.getDay(),
+      localToday,
+      totalWeekActivities: weekActivities.length,
+      activities: weekActivities.map(a => ({
+      id: a.id,
+      title: a.activitySnapshot?.title,
+      status: a.status,
+      dayOfWeek: a.dayOfWeek,
+      scheduledDate: a.scheduledDate?.toString(),
+      scheduledGetDay: a.scheduledDate?.getDay?.(),
+    }))
+  });
     return weekActivities.filter(a => {
       if (a.dayOfWeek !== undefined) {
         return a.dayOfWeek === scheduleDayOfWeek;
